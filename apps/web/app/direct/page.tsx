/**
 * Direct Control Surface (spec §6.5) — CRUD fallback when conversation
 * isn't the right interaction model for a task (e.g., bulk edits, settings).
 *
 * Substrate placeholder. Real implementation in Phase 2.
 */

export default function DirectPage(): JSX.Element {
  return (
    <section style={{ maxWidth: 960, margin: "0 auto", padding: "2rem 1.5rem" }}>
      <h1>Direct Control</h1>
      <p style={{ opacity: 0.6 }}>
        CRUD fallback surface. (Substrate placeholder.)
      </p>
    </section>
  );
}
