/**
 * Artifact Surface (spec §6.4) — generated artifacts the agent produces
 * (documents, reports, exports) viewable in-context.
 *
 * Substrate placeholder. Real implementation in Phase 2.
 */

export default function ArtifactPage(): JSX.Element {
  return (
    <section style={{ maxWidth: 960, margin: "0 auto", padding: "2rem 1.5rem" }}>
      <h1>Artifacts</h1>
      <p style={{ opacity: 0.6 }}>
        Generated artifacts appear here. (Substrate placeholder.)
      </p>
    </section>
  );
}
